{
  "name": "projectcraft",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run dev:backend & pnpm run dev:frontend",
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:backend": "cd backend && (test -d .venv || python3 -m venv .venv) && .venv/bin/pip install -q -r requirements.txt && .venv/bin/uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "pnpm --dir frontend build",
    "start": "pnpm --dir frontend start",
    "lint": "pnpm --dir frontend lint",
    "type-check": "pnpm --dir frontend exec tsc --noEmit",
    "env:init": "cp -n .env.local.example frontend/.env.local 2>/dev/null || cp .env.local.example frontend/.env.local"
  }
}
